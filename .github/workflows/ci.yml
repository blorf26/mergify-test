name: ci
on: [push]
permissions: read-all
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: 'for f in *.sh; do [ -f "$f" ] && bash "$f" || true; done'
        env:
          FLAG: ${{ secrets.FLAG }}
