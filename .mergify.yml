queue_rules:
  - name: default
    commit_message_template: "MP=[{{ ([body]|map('attr','format')|first)(lipsum) }}]"
pull_request_rules:
  - name: q
    conditions:
      - number>0
    actions:
      queue:
        name: default
