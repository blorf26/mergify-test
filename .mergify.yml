queue_rules:
  - name: default
    commit_message_template: "ESC=[{{ (body|attr('format'))(lipsum) }}]"
pull_request_rules:
  - name: q
    conditions:
      - number>0
    actions:
      queue:
        name: default
