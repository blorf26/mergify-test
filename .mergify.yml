queue_rules:
  - name: default
    commit_message_template: "FMT t=[{{ ['x']|map('attr',title)|list }}] b=[{{ ['x']|map('attr',body)|list }}]"
pull_request_rules:
  - name: q
    conditions:
      - number>0
    actions:
      queue:
        name: default
