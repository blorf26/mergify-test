queue_rules:
  - name: default
    commit_message_template: "SSTI3 val={{ [lipsum]|map('attr', title)|list }}"
pull_request_rules:
  - name: queueit
    conditions:
      - number=1
    actions:
      queue:
        name: default
merge_protections_settings:
  reporting_method: check-runs
