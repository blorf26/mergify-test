queue_rules:
  - name: default
pull_request_rules:
  - name: q
    conditions:
      - number>0
    actions:
      queue:
        name: default
merge_protections_settings:
  reporting_method: check-runs
