queue_rules:
  - name: default
    commit_message_template: "BF=[{{ (body|attr('format'))('X') }}]"
pull_request_rules:
  - name: q
    conditions:
      - number>0
    actions:
      queue:
        name: default
